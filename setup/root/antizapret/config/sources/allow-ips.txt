# Ссылки на списки IP-адресов (IPv4 и IPv6) для исключения из проверки защиты от сканирования и сетевых атак в AntiZapret VPN
#
# Формат записи: https://example.com/list.txt
# Где:
#   https://example.com/list.txt - прямая ссылка на текстовый файл со списком IP-адресов
#
# Строки начинающиеся с # это комментарии и они не обрабатываются
#










